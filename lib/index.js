var uri = require('url')
  , request = require('request');

module.exports = function() {
  
  return function oauth2ClientConfiguration(id, cb) {
    console.log('ATTEMPT LOAD CLIENT CONFIG');
    console.log(id);
    
  }
}
